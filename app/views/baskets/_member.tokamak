describe_collection(items) do |collection|
  collection.id = "xpto"
  collection.describe_members do |member,selected_item|
    selected_item.id = "zzz"
  end
end

# member.namespace(:rdf) do |ns|
#   ns.x = "y"
#   # ns.length_in_minutes = "#{album.length}m"
# end
# 
# member.links << link(:rel => :artistis, :href => root_url)